import React from "react";
import ReactDOM from "react-dom/client";
import MeetingBroadcast from "./MeetingBroadcast.jsx";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MeetingBroadcast />
  </React.StrictMode>
);
